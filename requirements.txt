whisperx
openai-whisper
anthropic
# Optional alternate LLM provider for Stage 3 / 3.5 (opt-in via
# LLM_PROVIDER=gemini). Anthropic remains the default.
google-genai
sounddevice
scipy
numpy
pyaudio
python-dotenv
flask
pyannote.audio

# Cloud invoker (cloud/transcribe_cloud.py) — calls the deployed Modal GPU app.
# Not needed for the legacy local runner, only for the cloud path.
modal
